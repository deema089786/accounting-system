import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  gridItem: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: 50,
  },
}));
